#!/bin/bash
#
#
#THIS_SCRIPT=$(/bin/readlink -nf "$0")

SCRIPT_PATH=$(cd `dirname ${0}`; pwd)
#echo $SCRIPT_PATH

export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:$SCRIPT_PATH/lib \
	$SCRIPT_PATH/triton4.0.0.x $*
